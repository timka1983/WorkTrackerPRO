# WorkTracker PRO 🚀

Профессиональная система учета рабочего времени.

## 🛠 Как разместить этот проект на GitHub (Пошагово)

### 1. Загрузка кода
Если вы скачали этот проект архивом:
1. Создайте пустой репозиторий на [GitHub](https://github.com/new).
2. В папке с проектом выполните в терминале:
   ```bash
   git init
   git add .
   git commit -m "First commit"
   git branch -M main
   git remote add origin https://github.com/ВАШ_ЛОГИН/ВАШ_РЕПОЗИТОРИЙ.git
   git push -u origin main
   ```

### 2. Настройка базы данных (Supabase)
Перейдите в **Settings -> Secrets and variables -> Actions** в вашем репозитории на GitHub и добавьте два секрета:
- `VITE_SUPABASE_URL` — ссылка на ваш проект.
- `VITE_SUPABASE_ANON_KEY` — ваш публичный API ключ.

### 3. Запуск сайта
1. Перейдите в **Settings -> Pages**.
2. В поле **Source** выберите **GitHub Actions**.
3. После этого при каждом обновлении кода (git push) ваш сайт будет автоматически обновляться по ссылке, указанной в этом разделе.

## 💻 Технологии
- **React 19** + **Vite**
- **Tailwind CSS** (дизайн)
- **Supabase** (база данных и синхронизация)
- **GitHub Actions** (авто-деплой)

## ⚠️ Важное замечание
Для корректной работы роутинга на GitHub Pages (чтобы при перезагрузке страницы не было 404), проект настроен на использование относительных путей. Если вы планируете использовать сложный роутинг (много страниц), рекомендуется деплоить на **Vercel** (инструкция есть в истории проекта).
